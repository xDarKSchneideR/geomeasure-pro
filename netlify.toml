[build]
  command = "npm run validate:frontend && npm run build"
  publish = "dist"

# Ejecutar validación de seguridad en cada deploy
[build.environment]
  NODE_VERSION = "20"

# Fail deploy si la validación falla
[[plugins]]
  package = "@netlify/plugin-lighthouse"

# Headers de seguridad (como te envié antes)
[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://unpkg.com; connect-src 'self' https://api.render.com; img-src * ; style-src 'self' 'unsafe-inline'"
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains"